// ============================================================
// PlanModeBar — Plan 模式指示条
//
// 当 Plan 模式激活时浮动显示，支持关闭。
// 当前为 UI 骨架。
// ============================================================

import React from 'react'

export default function PlanModeBar(): React.ReactElement | null {
  // UI 骨架 — plan mode 状态从 chatStore 读取后接入
  return null
}

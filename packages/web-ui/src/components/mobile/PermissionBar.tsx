// ============================================================
// PermissionBar — 浮动权限请求条
//
// 当 Agent 请求工具执行权限时显示（always-ask/accept-edits 模式）。
// 当前为 UI 骨架，实际权限流依赖 agent-server 协议。
// ============================================================

import React from 'react'

export default function PermissionBar(): React.ReactElement | null {
  // UI 骨架 — 实际权限流在 agent-server 协议完善后接入
  return null
}

// ============================================================
// AskUserBar — 底部用户问答输入条
//
// 替代 modal 弹窗，移动端用底部条显示 Agent 提问。
// 当前为 UI 骨架 — 需 chatStore _pendingQuestion 接入。
// ============================================================

import React from 'react'

export default function AskUserBar(): React.ReactElement | null {
  // UI 骨架 — chatStore._pendingQuestion 接入后启用
  return null
}

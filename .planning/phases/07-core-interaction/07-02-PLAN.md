---
phase: 07-core-interaction
plan: 07-02
wave: 1
depends_on: []
autonomous: true
objective: Implement @-mention file injection with fuzzy file picker dropdown, file content embedding into messages, and collapsible context blocks.
requirements:
  - MENTION-01
  - MENTION-02
  - MENTION-03
  - MENTION-04
  - MENTION-05
  - MENTION-06
files_modified:
  - src/renderer/components/chat/MentionPicker.tsx
  - src/renderer/components/chat/ChatPanel.tsx
  - src/renderer/components/chat/ChatMessage.tsx
  - src/renderer/stores/chat-store.ts
  - src/shared/types.ts
  - src/shared/ipc-channels.ts
  - src/preload/index.ts
  - src/main/ipc-handlers.ts
  - src/renderer/styles/chat.css
must_haves:
  truths:
    - "User types @ in chat input and sees fuzzy-searchable file picker dropdown"
    - "Selected file content is injected into the conversation as collapsible block"
    - "LLM receives file content as part of the user message context"
    - "Files over 100KB show a warning before injection"
    - "Fuzzy search works over relative file paths"
  artifacts:
    - path: "src/renderer/components/chat/MentionPicker.tsx"
      provides: "Fuzzy file picker dropdown triggered by @ in chat input"
      min_lines: 80
    - path: "src/shared/types.ts"
      provides: "FileMention type for embedded file content in messages"
      contains: "file_mention"
  key_links:
    - from: "src/renderer/components/chat/MentionPicker.tsx"
      to: "src/renderer/stores/workspace-store.ts"
      via: "File listing from workspace tree"
      pattern: "useWorkspaceStore"
    - from: "src/renderer/components/chat/ChatPanel.tsx"
      to: "src/renderer/components/chat/MentionPicker.tsx"
      via: "Rendered near chat input when @ is typed"
      pattern: "MentionPicker"
---

<objective>
@-mention file injection allowing users to embed specific file contents into AI conversations via a fuzzy-searchable file picker triggered by typing "@" in the chat input.

Purpose: Give users control over what code context the AI sees, enabling precise context injection without relying solely on the agent's file search tools.
Output: MentionPicker component, FileMention message type, file content IPC, collapsible context blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-core-interaction/07-CONTEXT.md
@.planning/phases/07-core-interaction/07-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add FileMention type, file read IPC, and chat-store mention support</name>
  <files>src/shared/types.ts, src/shared/ipc-channels.ts, src/preload/index.ts, src/main/ipc-handlers.ts, src/renderer/stores/chat-store.ts, src/shared/__tests__/ipc-channels.test.ts</files>
  <read_first>
    src/shared/types.ts
    src/shared/ipc-channels.ts
    src/preload/index.ts
    src/main/ipc-handlers.ts
    src/renderer/stores/chat-store.ts
    src/renderer/stores/workspace-store.ts
  </read_first>
  <behavior>
    - Test 1: FileMention type has type='file_mention', path, content, size fields
    - Test 2: file:read-content IPC channel validates and returns file content
    - Test 3: sendMessage with mentions includes file content in the formatted message
    - Test 4: Files over 100KB are rejected with appropriate error message
  </behavior>
  <action>
    Per CONTEXT.md: dropdown on @, collapsible block in sent msg, fuzzy search over relative paths, 100KB limit.

    1. Add to src/shared/types.ts:
       ```typescript
       export interface FileMention {
         type: 'file_mention';
         path: string;
         content: string;
         size: number;
       }
       ```
       Extend ChatMessage content to support mentions:
       - Add `mentions?: FileMention[]` field to ChatMessage interface

    2. Add IPC channel to src/shared/ipc-channels.ts:
       - `'file:read-content': 'file:read-content'`
       - Request: `{ filePath: string }` (relative to workspace root)
       - Response: `{ content: string; size: number; path: string }`
       - Add Zod schemas

    3. Add preload bridge in src/preload/index.ts:
       - `readFileContent: (request: { filePath: string }) => ipcRenderer.invoke('file:read-content', request)`

    4. Add IPC handler in src/main/ipc-handlers.ts:
       - `file:read-content` handler: resolve filePath relative to workspaceRoot, read file with fs.readFile, check size < 102400 (100KB), return content. If > 100KB, return error `{ error: 'File too large', size: actualSize, limit: 102400 }`.

    5. Extend chat-store.ts sendMessage:
       - Accept optional `mentions?: FileMention[]` parameter
       - When mentions are present, format them into the message content before sending to LLM:
         ```
         [Context from {path}]:
         {content}
         ---
         ```
       - Store mentions on the ChatMessage for rendering

    6. Add to chat-store state:
       - `pendingMentions: FileMention[]` — files staged for next message
       - `addMention(mention: FileMention)` action
       - `removeMention(path: string)` action
       - `clearMentions()` action (called after message sent)
  </action>
  <verify>
    <automated>cd E:/ai/wzxClaw && npx vitest run src/shared/__tests__/ipc-channels.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    - FileMention type defined in shared/types.ts
    - file:read-content IPC channel with 100KB limit
    - chat-store has pendingMentions, addMention, removeMention, clearMentions
    - sendMessage supports mentions parameter
  </done>
</task>

<task type="auto">
  <name>Task 2: MentionPicker component + ChatMessage collapsible blocks + CSS</name>
  <files>src/renderer/components/chat/MentionPicker.tsx, src/renderer/components/chat/ChatPanel.tsx, src/renderer/components/chat/ChatMessage.tsx, src/renderer/styles/chat.css</files>
  <read_first>
    src/renderer/components/chat/ChatPanel.tsx
    src/renderer/components/chat/ChatMessage.tsx
    src/renderer/stores/workspace-store.ts
    src/renderer/stores/chat-store.ts
    src/renderer/styles/chat.css
  </read_first>
  <acceptance_criteria>
    - grep "MentionPicker" src/renderer/components/chat/ChatPanel.tsx | wc -l (must be >= 1)
    - grep "file_mention\|FileMention\|mention-block" src/renderer/components/chat/ChatMessage.tsx | wc -l (must be >= 2)
    - grep "mention-picker\|mention-block" src/renderer/styles/chat.css | wc -l (must be >= 3)
    - npx vitest run passes
  </acceptance_criteria>
  <action>
    Per CONTEXT.md: dropdown on @, fuzzy search, collapsible block with [context] label, 100KB warning.

    1. Create src/renderer/components/chat/MentionPicker.tsx:
       - Positioned absolutely below the chat input when triggered by "@"
       - List of files from workspace-store's file tree, flattened to relative paths
       - Fuzzy filter as user types after "@" (use simple fuzzy matching or cmdk)
       - Each item shows relative path, with matching chars highlighted
       - Click or Enter selects file, calls readFileContent IPC, adds to pendingMentions
       - Escape closes the picker
       - Show file size next to path for awareness
       - If file > 100KB, show warning "File is {size}, exceeds 100KB limit" and disable selection
       - Props: `visible: boolean, position: { top: number, left: number }, onSelect: (file: FileMention) => void, onClose: () => void`

    2. Modify src/renderer/components/chat/ChatPanel.tsx:
       - Track "@" trigger state: `showMentionPicker` and picker position
       - In the chat input textarea, detect when user types "@" at start or after whitespace
       - Render MentionPicker when showMentionPicker is true
       - On mention select: add to pendingMentions, insert filename badge in input (e.g., `@filename.ts`)
       - On send: include pendingMentions in sendMessage call
       - Show pending mention badges above input area (small tags with X to remove)

    3. Modify src/renderer/components/chat/ChatMessage.tsx:
       - If message has mentions array, render collapsible blocks above the message text
       - Each block: header with filename and file size, collapsible content with syntax highlighting
       - Default collapsed, click to expand
       - Styled with distinct border (subtle accent) and "[context]" badge

    4. Add CSS to src/renderer/styles/chat.css:
       - `.mention-picker` — dropdown, max-height 300px, overflow-y auto, background #252526, border, z-index 100
       - `.mention-picker-item` — padding 6px 12px, hover background #2d2d2d
       - `.mention-picker-item .match` — highlight matching chars with bold + accent color
       - `.mention-badge` — inline badge in input area, background #3c3c3c, border-radius 3px
       - `.mention-block` — collapsible block in message, border-left 3px solid #007acc, margin 4px 0
       - `.mention-block-header` — flex row with filename, size, collapse toggle
       - `.mention-block-content` — pre-wrap code content, background #1e1e1e, max-height 400px overflow
  </action>
  <verify>
    <automated>cd E:/ai/wzxClaw && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - MentionPicker component with fuzzy search over workspace files
    - ChatPanel detects @ and shows picker
    - Selected files added as pending mentions with badges
    - ChatMessage renders mentions as collapsible context blocks
    - 100KB limit enforced with warning
    - All tests pass
  </done>
</task>

</tasks>

<verification>
1. Type @ in chat input, verify dropdown appears with file list
2. Type partial filename, verify fuzzy filtering works
3. Select a file, verify badge appears in input and mention is staged
4. Send message, verify collapsible context block appears above message text
5. Try to select a file > 100KB, verify warning is shown
6. Run full test suite: npx vitest run
</verification>

<success_criteria>
- @-mention picker appears on typing @ with fuzzy search
- File content injected into message and visible as collapsible block
- 100KB limit enforced
- LLM receives file content as formatted context
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-interaction/07-02-SUMMARY.md`
</output>

version: "3.8"

services:
  relay:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wzxclaw-relay
    restart: unless-stopped
    ports:
      - "127.0.0.1:8081:8080"
    environment:
      - AUTH_TOKEN=${AUTH_TOKEN}
      - PORT=8080

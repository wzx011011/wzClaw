---
name: resolve
description: 分析 git 合并冲突并提供解决方案
---

分析并解决当前的 git 合并冲突。

步骤：
1. 运行 `git status` 找到所有冲突文件
2. 对每个冲突文件，读取内容找到 `<<<<<<<` 标记
3. 分析冲突双方的意图
4. 提供合并建议，尽量保留双方的有效改动

规则：
- 先列出所有冲突文件
- 逐个分析冲突原因
- 给出具体的解决代码
- 最后运行 `git add` 标记已解决
